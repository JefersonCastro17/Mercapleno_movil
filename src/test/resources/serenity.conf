webdriver {
    driver = chrome
    autodownload = true
    wait.for.timeout = 10000

    capabilities {
        "goog:chromeOptions" {
            args = [
                "start-maximized",
                "incognito"
            ]
        }
    }
}

serenity {
    take.screenshots = FOR_EACH_ACTION
    logging = VERBOSE
}

