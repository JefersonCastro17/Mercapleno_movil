name: modulo_inventario
description: Minimal package for analyzing the inventory module.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  intl: ^0.19.0

flutter:
  uses-material-design: true
